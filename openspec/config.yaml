# openspec/config.yaml
# Auto-generated by sdd-init — 2026-06-28
schema: spec-driven

context: |
  Tech stack: React 19, TypeScript 6.0, Vite 8, Firebase 12 (Auth, Firestore, Storage)
  Architecture: SPA with lazy-loaded routes, context-based state (Auth, Toast, Confirm), Firebase-driven persistence
  Testing: No test runner installed; strict_tdd disabled by default
  Style: ESLint 10 (typescript-eslint recommended, react-hooks, react-refresh)
  PWA: vite-plugin-pwa with Workbox, CacheFirst for Unsplash images

rules:
  proposal:
    - Include rollback plan for risky changes
    - Scope each proposal to a single concern
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Specs in English unless UI copy is the subject
  design:
    - Include sequence diagrams for complex flows
    - Document architecture decisions with rationale
  tasks:
    - Group by phase, use hierarchical numbering
    - Keep tasks completable in one session
  apply:
    - Follow existing code patterns (context-based state, lazy routes, Firebase hooks)
    tdd: false
    test_command: ""
  verify:
    test_command: ""
    build_command: "npm run build"
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  strict_tdd: false
  runner:
    name: null
    command: null
  layers:
    unit: false
    integration: false
    e2e: false
  coverage:
    available: false
    command: null
  quality:
    linter:
      available: true
      command: npm run lint
    type_checker:
      available: true
      command: tsc -b
    formatter:
      available: false
      command: null
